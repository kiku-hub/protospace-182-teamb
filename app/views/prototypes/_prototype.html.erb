<div class="content_post" style="background-image: url(<%= prototype.image %>);">
  <div class="more">
    <span><%= image_tag prototype.image %></span>
    <%# ここのimage_tagの後ろ''を変数にすればいろんな画像載せれる %>
    <ul class="more_list">
      <li>
        <%= link_to '詳細', prototype_path(prototype) %>
      </li>
      <% if user_signed_in? && current_user.id == prototype.user_id %>
        <li>
          <%= link_to '編集', edit_prototype_path(prototype) %>
        </li>
        <li>
          <%= link_to '削除', prototype_path(prototype), data: { turbo_method: :delete } %>
        </li>
      <% end %>
    </ul>
  </div>

  <span class="name">
    <%= prototype.user.name %>
  </span>
</div>

<%# ここは部分テンプレート、親はshowhtml %>
